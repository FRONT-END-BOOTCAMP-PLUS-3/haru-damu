import type { SbCartRepository } from "@/infrastructure/repositories";

import type { CartDto } from "./dtos";
/*
class CartCreateUseCase {
  constructor(private readonly: SbCartRepository) {}

  async execute(cart: CartDto) {
    return await this.readonly.create(cart);
  }
}
*/
